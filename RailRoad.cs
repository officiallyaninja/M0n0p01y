namespace M0n0p01y;

public record RailRoad(string name, int position) : Property(name, SpaceType.RailRoad, position, 100, 200)
{
    public override int CalculateRent()
    {
        return 
    }
}
